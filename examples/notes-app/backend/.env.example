# OpenRouter API key — required to actually stream from the LLM.
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model slug. Default: qwen/qwen3.6-plus (confirmed available on OpenRouter).
# Any OpenRouter chat-completion model with tool-calling support will work.
OPENROUTER_MODEL=qwen/qwen3.6-plus

# Optional uvicorn binding (only consulted by `python -m notes_app`).
HOST=127.0.0.1
PORT=8000
